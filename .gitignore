node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
docs/*.pdf
docs/*.txt

# Server runtime state (deploy-spesifik, repo'ya girmemeli)
server/applications.json
server/rates-history.json
server/consumed-tokens.json

# Geliştirme notları + agent çıktıları (dahili — public repo'da görünmemeli)
# NOT: Mevcut tracked dosyaları kaldırmak için: `git rm --cached -r arastirma/`
arastirma/
